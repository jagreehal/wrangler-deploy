---
"wrangler-deploy": minor
---

Add agent-friendly CLI metadata, structured JSON output, `wd context` project defaults, richer machine-readable summaries for deploy and lifecycle commands, and a new `wd create vite` scaffold for greenfield Cloudflare Vite projects. Also document the new workflow in the docs site.
